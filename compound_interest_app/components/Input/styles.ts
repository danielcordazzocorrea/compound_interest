import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        height: 50,
        borderColor: '#2979FF',
        borderWidth: 3,
        borderRadius: 15,
        padding: 15,
        fontSize: 18,
        width: 300,
        color: '#2979FF', },
        
});